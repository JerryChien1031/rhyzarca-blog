name: Sync Markdown Posts to Google Blogger

on:
  push:
    branches:
      - main
    paths:
      - '_posts/**'
      - '_pages/**'
      - 'scripts/**'
      - '.github/workflows/sync.yml'
      - 'assets/**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: blogger-sync
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: 檢查專案程式碼
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      # -------------------------------------------------------------
      # 1. 樣式與腳本 CDN 快取自動刷新（Purge jsDelivr Cache）
      # -------------------------------------------------------------
      - name: 自動刷新 jsDelivr CDN 快取
        run: |
          echo "正在請求 jsDelivr 清除快取..."
          curl -s https://purge.jsdelivr.net/gh/jerrychien1031/rhyzarca-blog@main/assets/css/blogger.css || true
          curl -s https://purge.jsdelivr.net/gh/jerrychien1031/rhyzarca-blog@main/assets/js/blogger.js || true
          echo "✅ jsDelivr 快取清除請求已完成！"

      # -------------------------------------------------------------
      # 2. Python 環境設定與 Blogger 文章同步
      # -------------------------------------------------------------
      - name: 設定 Python 環境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安裝相依套件
        run: |
          python -m pip install --upgrade pip
          pip install python-frontmatter markdown beautifulsoup4 google-api-python-client google-auth-httplib2 google-auth-oauthlib python-dotenv

      - name: 還原授權檔案與環境變數
        env:
          CLIENT_SECRET: ${{ secrets.BLOGGER_CLIENT_SECRET }}
          TOKEN_JSON: ${{ secrets.BLOGGER_TOKEN }}
          BLOG_ID: ${{ secrets.BLOGGER_BLOG_ID }}
        run: |
          mkdir -p scripts
          echo "$CLIENT_SECRET" > scripts/client_secret.json
          echo "$TOKEN_JSON" > scripts/token.json
          echo "BLOGGER_BLOG_ID=$BLOG_ID" > scripts/.env

      - name: 執行 Blogger 同步腳本
        id: publish
        run: |
          cd scripts
          python sync_to_blogger.py

      - name: 清理敏感金鑰檔案
        if: always()
        run: |
          rm -f scripts/client_secret.json scripts/token.json scripts/.env

      - name: 回寫 Posts 與 Pages 真實 Blogger identity
        if: ${{ always() && steps.publish.outcome != 'skipped' }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -- '_posts/*.md' '_pages/*.md'
          if git diff --staged --quiet; then
            echo "沒有任何 Markdown 文章的 ID 發生變更，略過提交。"
          else
            git commit -m "chore(blog): auto-update Blogger identities [skip ci]"
            git push
          fi

